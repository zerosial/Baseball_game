const Gamestart = document.getElementById("gamestart");     // 게임 스타트 버튼

Gamestart.addEventListener('click',function () {            // 클릭시 렌덤한 숫자 생성
let answer = Math.floor(Math.random() * 1000);
});

